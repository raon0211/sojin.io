import { css } from '@emotion/react'
import { FONT_SIZE_TITLE, FONT_WEIGHT_TITLE } from '@sojin-components/constants'

export function typographyTitle() {
  return css`
    font-size: ${FONT_SIZE_TITLE};
    font-weight: ${FONT_WEIGHT_TITLE};
  `
}
